## 11286. 절댓값 힙

````
무조건 값을 받아가면서 한 번에 중첩하는 코드를 구현하려고 하지 말자.
정확하게 내가 원하는 값에 접근하는 것이 중요하다.
💡 heappush( , )의 인자로 리스트가 들어감으로써 리스트를 요소로 가지는 우선순위 큐를 구현할 수 있으며, hepqp.heappop( )의 우선순위 큐 탐색은 인덱스 순서로 이루어진다.
1. [0]절댓값이 작은 순서
2. [1]실제 값이 작은 순서
heappush(큐(리스트), [절댓값, 실제값])
으로 구현이 가능하다.
````
